'use strict';

/**
 * foundation-email controller
 */

const { createCoreController } = require('@strapi/strapi').factories;

module.exports = createCoreController('api::foundation-email.foundation-email');
