'use strict';

/**
 * volunteer-type controller
 */

const { createCoreController } = require('@strapi/strapi').factories;

module.exports = createCoreController('api::volunteer-type.volunteer-type');
